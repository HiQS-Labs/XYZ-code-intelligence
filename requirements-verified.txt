# Verified environment — the exact package set that passed Act 1
#
# Captured 2026-09-07 from the GH-11 Act 1 clone's .venv immediately before that clone was
# deleted. This is the environment in which all 5 marathon phases were approved, `validate.sh`
# returned OK, 45 tests passed, and the numbers in MEASUREMENTS/runs/2026-09-06-act1-roundtrip.md
# were produced. Python 3.11.15.
#
# This file is a RECORD, not the install source. `pyproject.toml` is the source of truth and pins
# the 7 direct dependencies; this file additionally pins the transitive set, which pyproject does
# not and should not.
#
# Why it exists: the transitive versions are load-bearing at least once. transformers 5.16.1 with
# torch 2.14.0 loads `cross-encoder/ms-marco-MiniLM-L-6-v2` without error and returns [nan, nan]
# — reproduced with plain AutoModelForSequenceClassification under both eager and sdpa attention,
# with all checkpoint tensors finite. That is why the default reranker is
# mixedbread-ai/mxbai-rerank-xsmall-v1. A future rebuild that silently moves transformers may hit
# a different version of that class of bug, and without this file there is nothing to diff against.
#
# To rebuild the verified environment:
#   python3.11 -m venv .venv && ./.venv/bin/pip install -r requirements-verified.txt
#   XYZ_PY=$PWD/.venv/bin/python bash prelaunch.sh   # warms model + grammar caches, writes the marker
#   bash validate.sh
#
annotated-doc==0.0.5
anyio==4.15.1
certifi==2026.7.22
click==8.5.0
cloudpickle==3.1.2
einops==0.8.2
filelock==3.32.5
fsspec==2026.7.0
h11==0.16.0
hf-xet==1.6.0
httpcore==1.0.9
httpx==0.28.1
huggingface_hub==1.30.0
idna==3.19
iniconfig==2.3.0
Jinja2==3.1.6
joblib==1.6.0
markdown-it-py==4.2.0
MarkupSafe==3.0.3
mdurl==0.1.2
mpmath==1.3.0
narwhals==2.25.0
networkx==3.6.1
numpy==2.4.6
packaging==26.3
pluggy==1.6.0
psutil==7.2.2
Pygments==2.21.0
pytest==9.1.1
PyYAML==6.0.3
regex==2026.9.3
rich==15.0.0
safetensors==0.8.0
scikit-learn==1.9.0
scipy==1.17.1
sentence-transformers==6.0.1
setuptools==84.0.0
shellingham==1.5.4
sqlite-vec==0.1.9
sympy==1.14.0
threadpoolctl==3.6.0
tokenizers==0.23.2
torch==2.14.0
tqdm==4.70.0
transformers==5.16.1
tree-sitter==0.26.0
tree-sitter-language-pack==1.16.1
typer==0.27.2
typing_extensions==4.16.0
